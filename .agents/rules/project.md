---
trigger: always_on
---

# GetRich 项目规则入口

本项目以 Codex 为主，Antigravity 共用根目录 `AGENTS.md` 的全部项目规则。
开始任务前读取并遵守以下文件；若引用未自动展开，主动打开仓库根目录 `AGENTS.md`。
按其中第 7 节读取和更新共享状态。规则只维护在 `AGENTS.md`，不在此复制正文。

@../../AGENTS.md
