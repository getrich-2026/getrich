[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gr-signal"
version = "0.1.2"
description = "GetRich signal workspace placeholder"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
py-modules = []
