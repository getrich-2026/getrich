# Next Plan

## 下一步：扩大真实数据验证

1. 决定是否把 `/home/quant/data` 同步或挂载到默认 `/data`。
2. 扩大 stock/etf/index 日线导入范围。
3. 有 future/option 数据后验证扩展元数据导入。
4. `kline_min1` 准备好后跑分钟线 scan/import smoke test。

## 后续再排

- 更细的质量阈值按品类配置。
