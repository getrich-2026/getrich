"""Asset plugin extension points.

The first implementation keeps asset-specific behavior in table selection and
quality rules. Future index/future/option plugins should register here instead
of changing the main pipeline.
"""

