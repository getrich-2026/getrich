# =============================================================================
# getrich-database 环境变量模板
# 复制为 .env 后填入真实值（.env 已被 .gitignore 忽略）。
# config.yaml 里以 *_env / ${VAR} 引用这些变量名，明文不落配置文件。
# =============================================================================

# ---- PostgreSQL / TimescaleDB ----
PGHOST=localhost
PGUSER=postgres
PGPASSWORD=

# ---- 银河 AmazingData (yinhe) ----
YINHE_USER=
YINHE_PASSWORD=
YINHE_HOST=

# ---- 米筐 rqdatac (ricequant) ----
RQ_LICENSE=

# ---- 华泰 INSIGHT (insight) ----
INSIGHT_USER=
INSIGHT_PASSWORD=
