# Web 前端

!!! info "Phase 2 文档"
    本页尚未编写（计划在 Phase 2 补完）。

## 计划内容

- Vite + React 19 + TypeScript 项目结构
- 路由树（react-router 7 + 守卫）
- `AuthContext` + 401 拦截器 + Refresh Token 轮换
- `@tanstack/react-query` 数据管理
- `react-hook-form` + `zod` 表单
- shadcn/ui + Radix UI 组件
- SSE 实时进度（`BacktestJobDetail` 等）
- 图表（ECharts 权益曲线 + Recharts 常规图）
- 4 层 XSS 防御（zod → Pydantic → bleach → CSP）
